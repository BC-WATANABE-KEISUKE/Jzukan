# Design.md — デザインシステム参照ガイド

今後このリポジトリ（にほんごずかん / Jzukan）で新しい画面を作る・既存画面を直す際は、必ず以下のデザインシステムを参照すること。

- **正のデザインシステム**: [Design_Guideline/Jzukan Design System.dc.html](Design_Guideline/Jzukan%20Design%20System.dc.html)（ブラウザで開いて確認）
- 本ファイルはその要点をMarkdownで書き起こしたクイックリファレンス。詳細な色見本・コンポーネントの実物確認は上記HTMLを直接開くこと。

## コンセプト

外国人児童向けの国語補助教材アプリ（GAS + React、iPad想定）。「教える」から「見せる」へ ── 文化背景の違いによる「言葉のスキマ」を、1枚の写真・イラスト・短い動画で直感的に補うという教材コンセプトに基づき、すべてのビジュアルは**大きく・丸く・力強く**、共有iPadでの誤操作を防ぐ設計を貫く。

- 対象: 小学校 外国人児童（美濃加茂市教育委員会）
- 実装: Google Apps Script + React 18 (CDN) + Tailwind
- 参照元: `index.html` / `コード.js`（プロジェクト添付コード）

## カラー

### ニュートラル（スレート）— 背景・本文・境界線

| 色 | 用途 |
|---|---|
| `#f8fafc` (slate-50) | 背景（本文） |
| `#f1f5f9` (slate-100) | 全体背景 |
| `#e2e8f0` (slate-200) | 境界線 |
| `#94a3b8` (slate-400) | 補助文字 |
| `#475569` (slate-600) | 本文 |
| `#0f172a` (slate-900) | 見出し |

### アクセント（インディゴ）— 主要アクション・強調

| 色 | 用途 |
|---|---|
| `#eef2ff` (indigo-50) | 淡色背景 |
| `#c7d2fe` (indigo-200) | 罫線・下線 |
| `#818cf8` (indigo-400) | — |
| `#6366f1` (indigo-500) | 基準色 |
| `#4f46e5` (indigo-600) | 濃色テキスト |

### セマンティック

| 色 | 用途 |
|---|---|
| `#f43f5e` (rose-500) | 「あえて収録していない」等の注意喚起 |
| `#fef2f2` + `border #fecaca` (red-50) | エラー画面背景 |
| `#0f172a` 80%不透明 (slate-900/80) | モーダル背景（ぼかし） |

### 学年パレット（oklch生成、本の背表紙用）

学年ごとに識別しやすい oklch の虹色パレット（例: 2ねんせい `oklch(66% .18 25)`、3ねんせい `oklch(82% .19 96)`、4ねんせい `oklch(66% .15 150)`、5ねんせい `oklch(60% .13 250)`）。各色は明るい base と濃い dark の2段階で構成し、背表紙のグラデーション（`linear-gradient(100deg, base 60%→white 0%, base 16%, base 78%, dark 100%)`）に使用。背景は全学年色を薄く混ぜた `oklch(96% .03 *)` の淡いグラデーション。

## タイポグラフィ

- **見出し書体（Klee One）**: 単語カードの見出し・ふりがな専用。手書き風で親しみやすい。ルビ（ふりがな）は漢字部分にだけ自動で振られる独自アルゴリズムを使用。
- **UI書体（sans-serif）**: それ以外全て。太いウェイト（`font-black` / `font-extrabold`）を多用し、子どもにもはっきり読める設計。
  - 見出し: 900 / 36px
  - 単元見出し: 900 / 24px
  - 本文（意味説明）: 800 / 18px
  - サブ・補助テキスト: 700 / 14px
  - 注記・免責文: 600 / 11px

## 形・余白・影

カード・ボタンともに大きな角丸を採用し、iPadでタップする対象であることを視覚的に示す。

- ボタン: 基本的に `rounded-full`（丸ボタン/ピル型）
- カード・モーダル: 2〜3rem の大きな角丸（`0.75rem` / `1rem` / `1.5rem`＝カード / `2rem`＝単語カード / `full`＝ボタン の5段階）

| シャドウ | 用途 |
|---|---|
| `shadow-md` | 単元カード・小コンポーネント |
| `shadow-lg` | 単語カード・主要ボタン |
| `shadow-2xl` | モーダル（もくじ・注意事項） |

## コンポーネント

- **ボタン**: 主要CTA（例: 「▦ もくじ」）はインディゴ背景＋白文字＋強い影。押下時は `active:scale-95` でわずかに縮むフィードバック。角丸は用途を問わず基本 full。
- **単語カード（WordCard）**: 上部に写真/イラスト/動画（16:9枠）、左上にKlee Oneのふりがな付きラベル、下部に意味説明文。横長画像は背景ぼかし＋前面containの二重表示。複数画像は左右の丸い矢印ボタンで送る。動画はGoogle Driveのiframeプレビュー。
- **単元カード（Home）**: 下ボーダーに淡いインディゴのアクセント線。
- **学年スパイン（本棚の本・GradeSpine）**: oklchグラデーションの背表紙。タップで press → cover（浮き上がり）→ opening（ページがめくれる）の3段階アニメーションで単元一覧へ遷移。
- **スティッキーヘッダー**: 半透明白＋ぼかし（`backdrop-filter: blur`）、下部に細い境界線。
- **モーダル（もくじ / 注意事項）**: 全画面をスレート900の80%透明＋ぼかしで覆い、モーダルは中央配置。閉じるボタンは常に右上の丸いXボタン。
- **セクション見出し**: 太い左ボーダー（`border-left: 8px solid indigo-500`）による強調。

## モーション・操作性

| 演出 | 内容 |
|---|---|
| 押下 (130ms) | `scale(0.94)` — ease |
| 浮き上がり (380ms) | `scale(1.22) translateY(-10px)` — `cubic-bezier(.25,.8,.3,1)` |
| 本が開く (480ms) | 8分割ストライプが遅延付き `rotateY` で開き、白フェードで画面遷移 |

通常のタップ操作はすべて `active:scale-95` の即時フィードバックのみ。学年選択だけ特別な「本を開く」演出を持つ、儀式的なエントリーポイント。

**共有iPad向けの操作制約**:
- ページ全体で text-select / タップハイライト / フォーカスリングを無効化（誤操作防止）
- viewport は `user-scalable=no` で固定、ピンチズーム不可
- 画像読み込み失敗やデータ取得エラー時は、赤系の専用エラー画面と「再読み込み」ボタンを表示

## コンテンツ・トーン

UI文言はひらがな中心で短く（「もくじ」「ねんせい」「じゅんび中」）、児童にもそのまま読める語を選ぶ。一方「この教材について」ページは大人（教員）向けの説明文で、収録／非収録の判断基準を丁寧な文体で明示する、というトーンの使い分けがある。

収録しない語彙の判断基準: ① 動作化で伝わるもの　② 教科書と価値が重複するもの　③ 写真入手が困難でイラスト化も理解を助けないもの　④ 中立性の観点からイラスト化を避けるべきもの

素材は生成AI（Google Gemini 等）で制作している旨を、学年選択画面下部に常時明記。教材の公開範囲・二次利用禁止についての注意事項をモーダルで案内する誠実な運用姿勢も、このアプリのトーンの一部。

## 新規画面実装時のガイドライン

1. 背景・本文・境界線は必ずスレート系、主要アクションのみインディゴで強調する（学年選択画面のみoklch虹色パレットの例外）。
2. ボタンは `rounded-full`、カード・モーダルは 1.5〜2rem 台の大きな角丸を基本とする。
3. 単語ラベル・ふりがな表示以外のUIテキストにKlee Oneを使わない（Klee Oneは単語カード専用）。
4. 通常のインタラクションは `active:scale-95` のみに留め、独自の複雑なアニメーションは学年選択（本を開く演出）のような特別な意味を持つ場合に限定する。
5. 共有iPadでの誤操作防止（text-select無効化・ピンチズーム不可・フォーカスリング無効化）を新規画面でも踏襲する。
